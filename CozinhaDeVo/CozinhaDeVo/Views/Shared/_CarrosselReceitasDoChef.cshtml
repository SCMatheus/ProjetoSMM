@using CozinhaDeVo.ViewModel.Home;

@model IEnumerable<CarrosselViewModel>



<div id="listaCarrosselChef" class="carousel slide" data-ride="carousel">
    <ol class="carousel-indicators">
        <li data-target="#listaCarrosselChef" data-slide-to="0" class="active"></li>
        @for (var i = 1; i < Model.Count(); i++)
        {
            <li data-target="#listaCarrosselChef" data-slide-to="@i"></li>
        }
    </ol>
    <div class="carousel-inner">
        @for (var i = 0; i < Model.Count(); i++)
        {
            if (i == 0)
            {
                <a href="@Model.ElementAt(i).UrlReceita">
                    <div class="carousel-item active">
                        <img class="d-block w-100" src="@Model.ElementAt(i).Imagem" alt="@Model.ElementAt(i).Titulo">
                        <div class="carousel-caption d-none d-md-block">
                            <h5>@Model.ElementAt(i).Titulo</h5>
                            <p>@Model.ElementAt(i).Descricao</p>
                        </div>
                    </div>
                </a>
            }
            else
            {
                <a href="@Model.ElementAt(i).UrlReceita">
                    <div class="carousel-item">
                        <img class="d-block w-100" src="@Model.ElementAt(i).Imagem" alt="@Model.ElementAt(i).Titulo">
                        <div class="carousel-caption d-none d-md-block">
                            <h5>@Model.ElementAt(i).Titulo</h5>
                            <p>@Model.ElementAt(i).Descricao</p>
                        </div>
                    </div>
                </a>
            }
        }
    </div>
    <a class="carousel-control-prev" href="#listaCarrosselChef" role="button" data-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="sr-only">Anterior</span>
    </a>
    <a class="carousel-control-next" href="#listaCarrosselChef" role="button" data-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="sr-only">Próximo</span>
    </a>
</div>